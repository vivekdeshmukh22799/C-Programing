#include <stdio.h>
void main(){
    //Use Truth table here in logical operators
    int a=0;
    int b=1;
    int c= a && b;
    int d= a || b;

    printf("Value of a && b = %d",c);
     printf("\nValue of a || b = %d",d);
}