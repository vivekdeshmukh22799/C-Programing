#include<stdio.h>
int main (){

// Area of circle formula A = π * radius2
int radius=5;
float area= 3.14 * 5 * 5;
printf("Area of circle = %f", area);

// Diameter of circle : D = 2 * radius
int Diameter=2*radius;
printf("\nDiameter of circle = %d", Diameter);

// Circumference of circle : A = 2 * π * r
float Circumference=2*3.14*radius;
printf("\nCircumference of circle = %f", Circumference);
  
}