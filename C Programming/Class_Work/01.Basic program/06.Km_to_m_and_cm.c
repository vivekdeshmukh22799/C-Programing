#include <stdio.h>
void main(){
    int km=10;
    int Convert_cm = km*100000;
    int convert_meter = km*1000;
    printf("\n%d km = %d cm",km, Convert_cm);
    printf("\n%d km = %d m",km, convert_meter);
}