#include<stdio.h>
void main (){

int length=15, breadth=13, Perimeter, Area, Diagonal;

// Perimeter of a Rectangle Formula	P = 2 (l + b)
Perimeter = 2 * (length+breadth);
printf("Perimeter of a Rectangle = %d",Perimeter);

// Area of a Rectangle Formula	A = l × b
Area=length*breadth;
printf("\nArea of a Rectangle = %d",Area);

// Diagonal of a Rectangle Formula	D = 2*l+2*b  ask to mam not getting this 
// Diagonal=(2*length+2*breadth)/2;
// printf("\nDiagonal of a Rectangle = %d",Diagonal);


}