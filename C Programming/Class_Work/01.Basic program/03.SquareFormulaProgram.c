#include<stdio.h>
void main(){

int side=5;

// Area of a Square = side * side;
int area=side*side;
printf("Area of a Square = %d", area);

// The perimeter of a Square =4*side
int perimeter=4*side;
printf("\nPerimeter of a Square = %d", perimeter);

// Diagonal of a d2=a2+a2
  int d, a=6;
  int Diagonal=(a*a)+(a*a)/(d*d);
// printf("\nDiagonal of a Square = %d", d);

}